SUBDIRS = gobject

BINDINGS_LIBS = gobject/libBindingsGObject.la

if ENABLE_PYTHON
SUBDIRS += python
endif

if SCRIPT
SUBDIRS += perl
endif

noinst_LTLIBRARIES = libBindings.la
libBindings_la_SOURCES = 
libBindings_la_LIBADD = $(BINDINGS_LIBS)
